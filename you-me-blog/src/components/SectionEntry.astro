---

---

<section class="entries">
  <slot/>
</section>

<style>
  .entries{
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(12rem, 100%), 1fr));
    gap: 3rem;
  }
</style>