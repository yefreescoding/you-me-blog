<section class="entries">
  <slot />
</section>

<style>
  .entries {
    /* margin: 5em 0; */
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(min(20em, 100%), 1fr));
    gap: 3rem;
  }
</style>
